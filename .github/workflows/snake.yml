name: Generate contribution-graph snake animation

on:
  # Every day at 00:10 UTC
  schedule:
    - cron: "10 0 * * *"
  # On push to main (to regenerate immediately after profile edits)
  push:
    branches:
      - main
  # Manual trigger
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Generate snake SVG (light + dark)
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: x-tahosin
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#58A6FF&color_dots=#0D1117,#1F6FEB,#58A6FF,#C9D1D9,#FFFFFF

      - name: Push SVG to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
